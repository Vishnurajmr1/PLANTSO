<!doctype html>
<html lang="en">
<!-- Head -->
<head>
    <!-- Page Meta Tags-->
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <meta name="keywords" content="">

    <!-- Favicon -->
    <link rel="apple-touch-icon" sizes="180x180" href="/images/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon/favicon-16x16.png">
    <link rel="mask-icon" href="/images/favicon/safari-pinned-tab.svg" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">

    <!-- Google Font-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />

    <!-- Vendor CSS -->
    <link rel="stylesheet" href="/stylesheets/libs.bundle.css" />

    <!-- Main CSS -->
     <link rel="stylesheet" href="/stylesheets/tailwind.css">

    <link rel="stylesheet" href="/stylesheets/theme.bundle.css" />





    <!-- Fix for custom scrollbar if JS is disabled-->
    <noscript>
        <style>
          /**
          * Reinstate scrolling for non-JS clients
          */
          .simplebar-content-wrapper {
            overflow: auto;
          }
        </style>
    </noscript>

    <!-- Page Title -->
    <title>{{pageTitle}}||</title>
</head>
<body>
     {{{body}}}
   
   <!-- Theme JS -->
    <!-- Vendor JS -->
    <script src="/javascripts/vendor.bundle.js"></script>
    
    <!-- Theme JS -->
    <script src="/javascripts/theme.bundle.js"></script>
    <script src="/javascripts/signup.js"></script>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="/javascripts/deleteModal.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.18/dist/sweetalert2.min.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.18/dist/sweetalert2.min.css">


{{!-- <script>
$(document).ready(function() {
  // When delete button is clicked
  $(".deleteButton").click(function(e) {
    e.preventDefault();
    //Get the form associated with the delete button
    const form=$(this).closest("form");
    //Get the category ID
    const categoryId=form.find("input[name='categoryId']").val();
    const productId=form.find("input[name='productId']").val();
    // Determine whether it's a category or product deletion
    const isCategory=categoryId!==undefined;
    const isProduct=productId!==undefined;
    console.log(`isProduct id:${productId}`);
    console.log(`isProduct:${isProduct}`);

    const id=isCategory? categoryId : productId;
    // const isCategory = form.find("input[name='categoryId']").length > 0; 
        // Show the corresponding modal
    $(`#deleteModal-${id}`).removeClass("hidden");
    //Handle the cancel button click
    $(`#deleteModal-${id}`).find('#cancelButton').click(function(){
      //Hide the modal
      $(`#deleteModal-${id}`).addClass('hidden'); 
    })
    // Handle the confirm delete button click
    $(`#deleteModal-${id}`).find("#confirmDeleteButton").click(function(){
      // Perform the form submission
      $('form').submit();
    })
    const modalTitle=isCategory?"Delete category":"Delete Product";
    const modalBody=isCategory?"Are you sure you want to delete this category":"Are you sure you want to delete this product?";
    $(`#deleteModal-${id}`).find('.modal-title').text(modalTitle);
    $(`#deleteModal-${id}`).find('.modal-body').text(modalBody); 

  });
});
</script> --}}
</body>

</html>
